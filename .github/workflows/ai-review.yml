name: AI Code Review

on:
  pull_request:
    branches: [main, develop]

permissions:
  contents: read
  pull-requests: write

jobs:
  review:
    name: AI Code Review
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Run AI code review
        uses: szaroket/AICodeReviewAction@6f66d6875dc45e31f182caa476b77a372e8be154 # SHA-pinned: 3rd-party action (supply-chain); GitHub-owned actions use major tags; repo renamed from ai-code-review-action
        with:
          pr-number: ${{ github.event.pull_request.number }}
          criteria-file: .github/review-criteria.md
          rules-file: AGENTS.md
          lessons-file: context/foundation/lessons.md
          scope-dirs: backend,frontend
          exclude: "**/uv.lock,**/package-lock.json,backend/migrations/**"
          anthropic-base-url: https://openrouter.ai/api
          anthropic-auth-token: ${{ secrets.OPENROUTER_API_KEY }}
          github-token: ${{ github.token }}
          publish: "true"
          format: all
          # Diff-only review converges in 2-4 turns; the headroom is for
          # submit_finding, which costs one turn per finding.
          max-turns: "15"

      - name: Upload review output
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: review-output
          path: review-output/
          retention-days: 7
